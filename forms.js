document.querySelectorAll('.lead-form').forEach(form=>{form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]');const original=btn.textContent;btn.disabled=true;btn.textContent='Sending…';const fd=new FormData(form);fd.append('_subject',form.dataset.mailSubject||'NordBridge website enquiry');fd.append('_template','table');fd.append('_captcha','false');try{const r=await fetch('https://formsubmit.co/ajax/nordbridge.dk@gmail.com',{method:'POST',headers:{'Accept':'application/json'},body:fd});if(!r.ok)throw new Error('send failed');form.reset();btn.textContent='Sent ✓';let note=form.querySelector('.form-status');if(!note){note=document.createElement('p');note.className='form-status';form.appendChild(note);}note.textContent='Thank you. Your enquiry has been sent to NordBridge.';}catch(err){btn.textContent=original;let note=form.querySelector('.form-status');if(!note){note=document.createElement('p');note.className='form-status';form.appendChild(note);}note.textContent='Could not send automatically. Please use WhatsApp or email nordbridge.dk@gmail.com.';}finally{setTimeout(()=>{btn.disabled=false;if(btn.textContent==='Sent ✓')btn.textContent=original;},5000);}});});